# DiffVisualizer — behaviour

<!-- GENERATED by the devkit (`make docs`) from this plugin's own e2e run.
     Do not edit by hand: every screenshot below is the asserted end state of a
     passing BDD scenario, so it cannot show behaviour the plugin no longer has.
     Captions are the scenario titles verbatim — reword them in tests/e2e/features/. -->

Each section is one scenario from `tests/e2e/features/`, with the screenshot the
test captured at its final assertion. 10 of 10 scenarios have one.

## Rendering diffs

### A diff block in the chat is rendered as a visual diff
<sub>BEH-1, BEH-3</sub>

![A diff block in the chat is rendered as a visual diff](screenshots/a-diff-block-in-the-chat-is-rendered-as-a-visual-diff.png)

### The rendered diff offers maximize and copy
<sub>BEH-2</sub>

![The rendered diff offers maximize and copy](screenshots/the-rendered-diff-offers-maximize-and-copy.png)

## Diff overlay, copy, and edge-case handling

### Maximizing a rendered diff opens a single fullscreen overlay
<sub>BEH-4</sub>

![Maximizing a rendered diff opens a single fullscreen overlay](screenshots/maximizing-a-rendered-diff-opens-a-single-fullscreen-overlay.png)

### The overlay closes via its close button and the inline diff survives
<sub>BEH-5</sub>

![The overlay closes via its close button and the inline diff survives](screenshots/the-overlay-closes-via-its-close-button-and-the-inline-diff-survives.png)

### The overlay closes via a backdrop click
<sub>BEH-5</sub>

![The overlay closes via a backdrop click](screenshots/the-overlay-closes-via-a-backdrop-click.png)

### The overlay closes via the Escape key
<sub>BEH-5</sub>

![The overlay closes via the Escape key](screenshots/the-overlay-closes-via-the-escape-key.png)

### Copying a rendered diff puts the raw diff text on the clipboard
<sub>BEH-6</sub>

![Copying a rendered diff puts the raw diff text on the clipboard](screenshots/copying-a-rendered-diff-puts-the-raw-diff-text-on-the-clipboard.png)

### A multi-file diff renders both files inside one visual diff
<sub>BEH-1</sub>

![A multi-file diff renders both files inside one visual diff](screenshots/a-multi-file-diff-renders-both-files-inside-one-visual-diff.png)

### Malformed diff text falls back to the readable plain code block
<sub>BEH-3</sub>

![Malformed diff text falls back to the readable plain code block](screenshots/malformed-diff-text-falls-back-to-the-readable-plain-code-block.png)

### A non-diff code fence is left untouched
<sub>BEH-1</sub>

![A non-diff code fence is left untouched](screenshots/a-non-diff-code-fence-is-left-untouched.png)
